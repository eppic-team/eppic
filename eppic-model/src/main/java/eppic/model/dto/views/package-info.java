/**
 * A package to contain the beans needed to provide the data views for
 * lattice graph and assembly diagram applets.
 * <p>
 * Essentially it is the same data available elsewhere but in a different shape ready to
 * be consumed by the client code in the lattice graph and assembly diagram.
 *
 * @since 3.1.0
 */
package eppic.model.dto.views;