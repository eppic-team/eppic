#!/bin/sh

dir=`dirname $0`

uniprotjar=$dir/../lib/uniprotjapi.jar
eppicjar=`ls $dir/../lib/uber-eppic*.jar`

if [ ! -f "$uniprotjar" ]
then
        echo "$uniprotjar is missing in $dir/../lib directory. Can't run eppic without it."
        exit 1
fi

java -cp $uniprotjar:$eppicjar eppic.Main $*
